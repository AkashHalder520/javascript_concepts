//js is a synchronous language
// its a single thereaded leanguages
// setTimeout

console.log("hi");

setTimeout(() => {
  console.log("akash");
}, 5000);  // if time is given as zero then also it will execute at last
console.log("my name is");

// next move to callback file 